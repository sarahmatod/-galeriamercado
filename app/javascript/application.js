// Entry point for the build script in your package.json
import "@hotwired/turbo-rails"
import "./controllers"
import "bootstrap"

import "./src/jquery"
import "jquery-ui"



/* $(document).on('scroll', function () {

  var pixelsFromTop = $(document).scrollTop()
  console.log(pixelsFromTop)

  if (pixelsFromTop < 4100) {
    $('body').css('background-color', '#FFFBF5')
  }
  else if (pixelsFromTop < 5200) {
    $('body').css('background-color', '#032B17')
  }
  else if (pixelsFromTop < 7000) {
    $('body').css('background-color', '#FFFBF5')
  }
  else if (pixelsFromTop < 11000) {
    $('body').css('background-color', '#032B17')
  }
  else {
    $('body').css('background-color', '#')
  }

}) */
